#[cfg(test)]
mod tests {
    use crate::problems::problem001::problem001;
    use crate::problems::problem002::problem002;
    use crate::problems::problem003::problem003;
    use crate::problems::problem004::problem004;
    use crate::problems::problem005::problem005;
    use crate::problems::problem006::problem006;
    use crate::problems::problem007::problem007;
    use crate::problems::problem008::problem008;
    use crate::problems::problem009::problem009;
    use crate::problems::problem010::problem010;
    use crate::problems::problem011::problem011;
    use crate::problems::problem012::problem012;
    use crate::problems::problem013::problem013;
    use crate::problems::problem014::problem014;
    use crate::problems::problem015::problem015;
    use crate::problems::problem016::problem016;
    use crate::problems::problem017::problem017;
    use crate::problems::problem018::problem018;
    use crate::problems::problem019::problem019;
    use crate::problems::problem020::problem020;
    use crate::problems::problem021::problem021;
    use crate::problems::problem022::problem022;
    use crate::problems::problem023::problem023;
    use crate::problems::problem024::problem024;
    use crate::problems::problem025::problem025;
    use crate::problems::problem026::problem026;
    use crate::problems::problem027::problem027;
    use crate::problems::problem028::problem028;
    use crate::problems::problem029::problem029;
    use crate::problems::problem030::problem030;
    use crate::problems::problem031::problem031;
    use crate::problems::problem032::problem032;

    #[test]
    fn test_problem001() {
        let result = problem001(1000);
        assert_eq!(result, 233168);
    }

    #[test]
    fn test_problem002() {
        let result = problem002();
        assert_eq!(result, 4613732);
    }

    #[test]
    fn test_problem003() {
        let result = problem003(600851475143);
        assert_eq!(result, 6857);
    }

    #[test]
    fn test_problem004() {
        let result = problem004(1000);
        assert_eq!(result, 906609);
    }

    #[test]
    fn test_problem005() {
        let result = problem005(20);
        assert_eq!(result, 232792560);
    }

    #[test]
    fn test_problem006() {
        let result = problem006(100);
        assert_eq!(result, 25164150);
    }

    #[test]
    fn test_problem007() {
        let result = problem007(10000);
        assert_eq!(result, 104743);
    }

    #[test]
    fn test_problem008() {
        let result = problem008();
        assert_eq!(result, 23514624000);
    }

    #[test]
    fn test_problem009() {
        let result = problem009(1000);
        assert_eq!(result, 31875000);
    }

    #[test]
    fn test_problem010() {
        let result = problem010(2000000);
        assert_eq!(result, 142913828922);
    }

    #[test]
    fn test_problem011() {
        let result = problem011();
        assert_eq!(result, 70600674);
    }

    #[test]
    fn test_problem012() {
        let result = problem012();
        assert_eq!(result, 76576500);
    }

    #[test]
    fn test_problem013() {
        let result = problem013();
        assert_eq!(result, "5537376230");
    }

    #[test]
    fn test_problem014() {
        let result = problem014();
        assert_eq!(result, 837799);
    }

    #[test]
    fn test_problem015() {
        let result = problem015();
        assert_eq!(result, 137846528820);
    }

    #[test]
    fn test_problem016() {
        let result = problem016();
        assert_eq!(result, 1366);
    }

    #[test]
    fn test_problem017() {
        let result = problem017();
        assert_eq!(result, 21124);
    }

    #[test]
    fn test_problem018() {
        let result = problem018();
        assert_eq!(result, 1074);
    }

    #[test]
    fn test_problem019() {
        let result = problem019();
        assert_eq!(result, 171);
    }

    #[test]
    fn test_problem020() {
        let result = problem020();
        assert_eq!(result, 648);
    }

    #[test]
    fn test_problem021() {
        let result = problem021();
        assert_eq!(result, 31626);
    }

    #[test]
    fn test_problem022() {
        let result = problem022();
        assert_eq!(result, 871198282);
    }

    #[test]
    fn test_problem023() {
        let result = problem023();
        assert_eq!(result, 4179871);
    }

    #[test]
    fn test_problem024() {
        let result = problem024();
        assert_eq!(result, 2783915460);
    }

    #[test]
    fn test_problem025() {
        let result = problem025();
        assert_eq!(result, 4782);
    }

    #[test]
    fn test_problem026() {
        let result = problem026();
        assert_eq!(result, 983);
    }

    #[test]
    fn test_problem027() {
        let result = problem027();
        assert_eq!(result, -59231);
    }

    #[test]
    fn test_problem028() {
        let result = problem028();
        assert_eq!(result, 669171001);
    }

    #[test]
    fn test_problem029() {
        let result = problem029();
        assert_eq!(result, 9183);
    }

    #[test]
    fn test_problem030() {
        let result = problem030();
        assert_eq!(result, 443839);
    }

    #[test]
    fn test_problem031() {
        let result = problem031();
        assert_eq!(result, 73682);
    }

    #[test]
    fn test_problem032() {
        let result = problem032();
        assert_eq!(result, 45228);
    }
}
